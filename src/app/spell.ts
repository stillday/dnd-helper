export interface Spell {
  name: string;
  id: number;
  caster: string; // ToDo: rename class 
  grad: number;
  // time: string;
  // range: string;
  // components: string;
  // duration: string;
  // damage: string;
  // rescue: string;
  // page: number;
}
