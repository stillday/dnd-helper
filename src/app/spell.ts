export interface Spell {
  name: string;
  id: number;
  class: string;
  grad: number;
  // time: string;
  // range: string;
  // components: string;
  // duration: string;
  // damage: string;
  // rescue: string;
  // page: number;
}
